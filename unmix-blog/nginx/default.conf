upstream blog-server {
    server blog-gunicorn:8000;
}

server {
    listen   80;

    location / {        
        proxy_pass http://blog-server;
    }

    location /static/ {
        alias   /app/static/;
    }

    location /media/ {
        alias   /app/media/;
    }
}